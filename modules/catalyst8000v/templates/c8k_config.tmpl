Section: IOS configuration
hostname ${cfg.c8k.hostname}
ipv6 unicast-routing
interface GigabitEthernet1
  mtu 8896 ! maximum MTU on GCP
  ip address dhcp
  ip nat outside
  negotiation auto
  ipv6 address dhcp
  ipv6 enable
  ipv6 nd autoconfig default-route
  ipv6 nd ra suppress all
end
